<% form_for @song do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name, :value => "Song #{Song.all.size + 1}" %>
  </p>

  <p><%= f.submit %></p>
<% end %>
